# Nowlert v1.9.6 release notes

## Release summary

Nowlert v1.9.6 completes the Teams and Discord presentation work started in
v1.9.4. Every integration now uses an exact official vendor asset rather than
a generated initials badge. Teams and Discord share the same device/event,
context, message, metric, detail, status, and source-time semantics while
Discord retains its richer source-specific fields.

Existing valid webhooks, routes, endpoints, secrets, and parsers remain
compatible. The source timestamp is still authoritative, but timezone-aware
instants now deliberately display in the local Nowlert machine clock. No
secret rotation is required.

## Changed

- Added official, locally hosted assets for Xen Orchestra, Grafana, Portainer,
  Proxmox VE, QNAP, Synology, TrueNAS, UniFi Network, UniFi Protect, UniFi
  Drive, Zabbix, Redfish, Supermicro, HPE, Dell, Home Assistant, and generic
  Nowlert events.
- Normalized each asset to a transparent 256 px PNG suitable for the 48 px
  Teams header slot.
- Recorded the official source and transformation notes for every asset in
  `assets/icons/README.md`.
- Added the optional `NOWLERT_ICON_BASE_URL` environment override for
  immutable branch previews or installations that mirror the same assets.
- Included the official icon directory in the container image and changed
  Discord delivery to upload the selected PNG as a webhook attachment. This
  keeps every integration thumbnail—including Grafana and Redfish—independent
  of Discord's ability to fetch a remote image URL.
- Preserved infrastructure identifiers and acronyms such as `PVE-01`, `CPU`,
  and `VMID` instead of title-casing them.
- Removed duplicated UniFi state, source context, and pin icons, and shortened
  the Network last-device label.
- Standardized all 17 Discord presentations on the same hierarchy used by
  Teams: `device • event`, integration/state/source context, event message,
  Severity/Category/Event time metrics, rich details, and official thumbnail.
- Added a shared Discord renderer that preserves integration-specific fields
  while enforcing the 25-field and 6,000-character embed limits.
- Timezone-aware source timestamps and epochs now render in the Nowlert
  machine/container local timezone by default. Naive source values remain
  local wall clocks, missing source time is omitted, and receipt time is never
  substituted. An optional IANA override supports the future WebUI.
- Preserved native UniFi Network and Protect epoch values until the shared
  Teams/Discord renderer applies the configured source-time policy.

## Fixed

- Xen Orchestra cards use the triggered backup name, omit missing Duration or
  Result values, and show explicit results such as `3 of 3 VMs successful` in
  both outputs. Per-VM size, speed, repository, and errors remain available in
  Discord.
- Added Teams-like Discord spacing with no synthetic blank rows: context,
  non-wrapping rule, unlabelled dark event highlight, horizontal
  Severity/Category/Event time row, another rule, vertical event details, and
  one footer separator immediately after the last detail. Removed the
  redundant labelled Event field and wrapped rule fragments.
- Fixed Home Assistant string tags being rendered one character at a time.
- Added an exact, configurable Dell iDRAC filter for trusted session audit
  records (`USR0030` and `USR0032`) across REDFISH/IPMI transports without
  hiding failed logins or other security events. Dell session cards also use
  concise event names and normalized legacy device contexts.
- Placeholder values such as `PASTE_HERE`, malformed URLs, credential-bearing
  URLs, and non-HTTPS Teams webhooks are rejected before an outbound request.

## Tracking

- Official vendor assets: #75
- Responsive Discord cards: #76
- Machine-local source timestamps: #77
- UniFi Protect device aliases: #78
- Trusted iDRAC audit suppression: #79
- Release coordination: #80

## Upgrade from v1.9.4

1. Back up the production configuration and stack definition.
2. Pull `fortpt/nowlert:1.9.6` or `ghcr.io/fortpt/nowlert:1.9.6` only after
   the release workflow succeeds.
3. Change only the image tag and recreate the container, preserving mounts,
   ports, routes, secrets, and proxy configuration.
4. Confirm the banner reports Nowlert 1.9.6 and both listeners are healthy.
5. Send one representative event for every configured Teams and Discord
   integration and verify its official logo, hierarchy, source time, and
   optional details.

## Rollback

Pin the image back to `fortpt/nowlert:1.9.4` (or the equivalent GHCR image)
and recreate the container with unchanged configuration and secrets.

## Validation required before release

- The complete automated parser, API, formatter, routing, security, release,
  and backwards-compatibility suite passes: 678 tests.
- A branch preview must be deployed on the office VM and all 17 Teams
  presentations must be delivered and visually reviewed before merge or tag.
- The same branch preview must deliver all 17 Discord presentations to the
  NOWLERT-HOST development destination for visual review before merge or tag.
